spring:
  data:
    mongodb:
      uri: mongodb://mongo:27017/recommendation_test_db
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

  cache:
    type: simple
  main:
    allow-bean-definition-overriding: true

eureka:
  client:
    enabled: false
