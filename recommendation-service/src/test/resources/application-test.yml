spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/recommendation_test_db
  cache:
    type: simple
  main:
    allow-bean-definition-overriding: true

eureka:
  client:
    enabled: false
